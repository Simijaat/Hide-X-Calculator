--- app/src/main/java/com/example/vaultcalc/ui/browser/BrowserScreen.kt
+++ app/src/main/java/com/example/vaultcalc/ui/browser/BrowserScreen.kt
@@ -213,8 +213,8 @@
                         horizontalArrangement = Arrangement.SpaceBetween
                     ) {
-                        IconButton(onClick = onNavigateBack) {
-                            Icon(Icons.Default.ArrowBack, contentDescription = "Back", tint = Color.White)
+                        IconButton(onClick = { /* Remove direct back button if user wants minimize, but we map back to minimize anyway */ onNavigateBack() }) {
+                            Icon(Icons.Default.ArrowDropDown, contentDescription = "Minimize", tint = Color.White)
                         }
                         IconButton(
                             onClick = {
@@ -428,8 +428,8 @@
             horizontalArrangement = Arrangement.SpaceBetween,
             verticalAlignment = Alignment.CenterVertically
         ) {
-            IconButton(onClick = onNavigateBack) {
-                Icon(Icons.Default.ArrowBack, contentDescription = "Back", tint = Color.White)
+            IconButton(onClick = { /* Map back to minimize */ onNavigateBack() }) {
+                Icon(Icons.Default.ArrowDropDown, contentDescription = "Minimize", tint = Color.White)
             }
             IconButton(onClick = onShowTabs) {
